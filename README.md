基于stm32f407zgt6的2.4寸tft_lcd触摸屏驱动
1.初版调通SPI_DMA驱动（HAL库函数），可实现刷屏，数字字符串，图片显示。
  spi通讯速率为10M  i2c通讯速率为200k  
2.驱动实现宏控是否使用DMA (#define USE_DMA)
3.待优化    （1）触控时而无反应     思路：硬件pfc座及杜邦线连接
            （2）触控IC未开启手势识别功能
            （3）cs，rs等引脚控制采用hal库函数控制，未来可优化为直接操作寄存器，可进一步提高刷屏速度
            （4）当前长按短按识别，采用的为轮询touch_count变量变化加读取设备ID判断，无法通过直接读状态寄存器值进行状态判断（原因为中断置位后，读取状态寄存器值，由于有时间延迟，很难读到（1：按下和3：抬起））
